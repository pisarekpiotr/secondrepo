package com.kodilla.good.patterns.challenges;

public class OrderService {
    public boolean order(User user, ProductName productName, ProductType productType, ProductPrice productPrice, ProductQuantity productQuantity) {
        return true;
    }
}
